language: node_js
node_js:
    - lts/*
    - node
dist: trusty
sudo: false
cache:
    directories:
        - ~/.npm
        - ~/.cache

before_install:
    - chmod +x ./setup.sh
    - ./setup.sh

install:
    - npm ci
